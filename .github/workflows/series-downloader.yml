name: Series Downloader
on:
  workflow_dispatch:
jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Python Dependencies
        run: pip install gspread google-auth pymediainfo requests requests-toolbelt pgsrip
      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y aria2 mediainfo ffmpeg mkvtoolnix tesseract-ocr tesseract-ocr-eng
      - name: Run Series Downloader
        env:
          GOOGLE_SHEETS_JSON: ${{ secrets.GOOGLE_SHEETS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          VIDARA_API_KEY: ${{ secrets.VIDARA_API_KEY }}
          BEAM_ADMIN_EMAIL: ${{ secrets.BEAM_ADMIN_EMAIL }}
          BEAM_ADMIN_PASSWORD: ${{ secrets.BEAM_ADMIN_PASSWORD }}
          IA_ACCESS_KEY: ${{ secrets.IA_ACCESS_KEY }}
          IA_SECRET_KEY: ${{ secrets.IA_SECRET_KEY }}
        run: python series-downloader.py
